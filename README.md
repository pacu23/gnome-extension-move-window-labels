# gnome-extension-move-window-labels

Copied the code from Native Window Placement extension that moves the window label while in Overview, to be able to achieve that without the extension. When using other extensions, the labels sometimes get obstructed, that's why one might desire this.
